const objeto = {
    LOADING: {loading: true},
    edad: 35,
    numero: 2,
    gafas: true
}

const variable = "LOADING";

//! coge la propiedad variable
console.log(objeto.variable);

//! coge la propiedad del valor de la variable -> name
console.log(objeto[variable]);